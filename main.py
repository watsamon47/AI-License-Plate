from ultralytics import YOLO
import easyocr
import cv2
import os
import requests 
import re
import difflib

# ===============================
# ตั้งค่า API
# ===============================
# ใส่ URL ที่ได้จาก Google Apps Script (Deploy > Web App)
API_URL = "https://script.google.com/macros/s/AKfycbxKZIoVrcfRZT620ODb2MWNoVYWOsoCehrDmn8eLMAXCc0X3FuvSUidu0YTnYMOzlCY/exec" 
API_KEY = "ASHI_CREAM" 

# ===============================
# รายชื่อจังหวัดทั้งหมด (สำหรับตรวจสอบความถูกต้อง)
# ===============================
THAI_PROVINCES = [
    "กรุงเทพมหานคร", "กระบี่", "กาญจนบุรี", "กาฬสินธุ์", "กำแพงเพชร", "ขอนแก่น", "จันทบุรี", "ฉะเชิงเทรา", "ชลบุรี", "ชัยนาท",
    "ชัยภูมิ", "ชุมพร", "เชียงราย", "เชียงใหม่", "ตรัง", "ตราด", "ตาก", "นครนายก", "นครปฐม", "นครพนม", "นครราชสีมา",
    "นครศรีธรรมราช", "นครสวรรค์", "นนทบุรี", "นราธิวาส", "น่าน", "บึงกาฬ", "บุรีรัมย์", "ปทุมธานี", "ประจวบคีรีขันธ์",
    "ปราจีนบุรี", "ปัตตานี", "พระนครศรีอยุธยา", "พะเยา", "พังงา", "พัทลุง", "พิจิตร", "พิษณุโลก", "เพชรบุรี", "เพชรบูรณ์",
    "แพร่", "ภูเก็ต", "มหาสารคาม", "มุกดาหาร", "แม่ฮ่องสอน", "ยโสธร", "ยะลา", "ร้อยเอ็ด", "ระนอง", "ระยอง", "ราชบุรี",
    "ลพบุรี", "ลำปาง", "ลำพูน", "เลย", "ศรีสะเกษ", "สกลนคร", "สงขลา", "สตูล", "สมุทรปราการ", "สมุทรสงคราม", "สมุทรสาคร",
    "สระแก้ว", "สระบุรี", "สิงห์บุรี", "สุโขทัย", "สุพรรณบุรี", "สุราษฎร์ธานี", "สุรินทร์", "หนองคาย", "หนองบัวลำภู",
    "อ่างทอง", "อำนาจเจริญ", "อุดรธานี", "อุตรดิตถ์", "อุทัยธานี", "อุบลราชธานี", "ยะลา", "เบตง"
]

# ฟังก์ชัน: แก้คำผิดจังหวัด (Fuzzy Matching)
def correct_province_name(ocr_text):
    if not ocr_text or len(ocr_text) < 2: return "ไม่ระบุ"
    
    # ใช้ difflib หาคำที่ใกล้เคียงที่สุดใน List จังหวัด
    # cutoff=0.4 แปลว่าต้องเหมือนกันเกิน 40% ถึงจะยอมรับ (ปรับเลขนี้ได้ 0.1-1.0)
    matches = difflib.get_close_matches(ocr_text, THAI_PROVINCES, n=1, cutoff=0.4)
    
    if matches:
        print(f"✨ แก้คำผิด: '{ocr_text}' -> '{matches[0]}'")
        return matches[0] # คืนค่าจังหวัดที่ถูกต้อง
    
    return ocr_text # ถ้าไม่เจอที่ใกล้เคียงเลย ให้ส่งค่าเดิมไป
# ===============================
# ฟังก์ชันส่งข้อมูลเข้า Dashboard
# ===============================
# --- แก้ไขช่วงบรรทัด 18-30 ---

# 1. เพิ่มตัวแปร province ในวงเล็บ
def send_to_dashboard(plate_number, province):
    """
    ส่งเลขทะเบียนและจังหวัดไปตรวจสอบสิทธิ์และบันทึก Log ลง Google Sheet
    """
    print(f"🚀 กำลังส่งข้อมูล: {plate_number} | {province} ไปยัง Dashboard...")

    try:
        # ใช้ action='check' เพื่อบันทึก Log 
        # 2. เพิ่ม key "province" ลงไปใน params
        response = requests.get(API_URL, params={
            "action": "check",
            "license": plate_number,
            "province": province,  # <--- เพิ่มบรรทัดนี้สำคัญมาก!
            "key": API_KEY
        })
        
        if response.status_code == 200:
            data = response.json()
            result = data.get('access', 'Unknown')
            owner = data.get('data', {}).get('owner', '-')
            
            if result == "Allow":
                print(f"✅ ผลลัพธ์: อนุญาต (Allow) | เจ้าของ: {owner}")
            else:
                print(f"⛔ ผลลัพธ์: ปฏิเสธ (Deny) | ติดต่อ รปภ.")
            return True
        else:
            print(f"❌ Server Error: {response.status_code}")
            return False

    except Exception as e:
        print(f"⚠️ เชื่อมต่อเน็ตไม่ได้: {e}")
        return False

# ===============================
# เริ่มต้นระบบ AI
# ===============================
model = YOLO("license_plate.pt")
reader = easyocr.Reader(['th', 'en'], gpu=False)

image_folder = "images"
valid_ext = (".jpg", ".jpeg", ".png")

print("--- เริ่มต้นระบบตรวจจับ ---")

for filename in os.listdir(image_folder):
    if not filename.lower().endswith(valid_ext):
        continue

    image_path = os.path.join(image_folder, filename)
    print(f"\n📷 กำลังประมวลผลรูป: {filename}")

    img = cv2.imread(image_path)
    if img is None: continue

    # 1) YOLO Detect
    results = model(img)

    for r in results:
        for box in r.boxes:
            cls_id = int(box.cls[0])
            if model.names[cls_id] != "license_plate":
                continue

            x1, y1, x2, y2 = map(int, box.xyxy[0])
            h, w, _ = img.shape
            x1, y1 = max(0, x1), max(0, y1)
            x2, y2 = min(w, x2), min(h, y2)

            plate_img = img[y1:y2, x1:x2]
            if plate_img.size == 0: continue

            # ---------------------------------------------------------
            # 2) OCR Read (แก้ไขใหม่: คัดแยก หมวด/เลข/จังหวัด ให้แม่นยำ)
            # ---------------------------------------------------------
            texts = reader.readtext(plate_img)

            # ตัวแปรเก็บค่า
            cat_part = ""    # หมวดอักษร (เช่น 9กฬ)
            num_part = ""    # เลขทะเบียน (เช่น 5384)
            province_part = "" # จังหวัด (เช่น กรุงเทพมหานคร)

            # วนลูปเช็คข้อความทีละก้อนที่ AI อ่านได้
            for t in texts:
                txt = t[1].strip()
                
                # กรองอักขระแปลกปลอมออก (เช่น . - |)
                txt = re.sub(r'[^\w\s]', '', txt) 

                if txt == "": continue

                # 1. เช็คว่าเป็น "เลขทะเบียน" ล้วนๆ หรือไม่ (มีแต่ตัวเลข 1-4 หลัก)
                if re.match(r"^\d{1,4}$", txt):
                    # ถ้าเจอตัวเลข ให้เก็บเป็นเลขทะเบียน (เลือกตัวที่ยาวกว่า กรณีเจอเศษเลข)
                    if len(txt) > len(num_part):
                        num_part = txt
                
                # 2. เช็คว่าเป็น "จังหวัด" หรือไม่ (ภาษาไทยยาวๆ ไม่มีตัวเลขผสม)
                elif re.match(r"^[ก-๙\s]+$", txt) and len(txt) > 2:
                    # ถ้าไม่ใช่หมวดอักษร (หมวดอักษรมักสั้นๆ 2-3 ตัว) ให้ถือเป็นจังหวัด
                    # ดักเพิ่ม: คำว่า "กก" หรือหมวดอื่นๆ อาจหลุดมา ต้องเช็คความยาว
                    if len(txt) > 3 or "กรุง" in txt or "จังหวัด" in txt:
                        province_part += txt

                # 3. เช็คว่าเป็น "หมวดอักษร" (มีภาษาไทยปนตัวเลข หรือไทยล้วนสั้นๆ)
                else:
                    # เช่น 9กฬ, กก, 1กข (มักจะมี ก-ฮ ปนอยู่)
                    if re.search(r"[ก-ฮ]", txt):
                        cat_part += txt

            # --- ประกอบร่างใหม่ ---
            
            # กรณีที่อ่าน 9กฬ5384 ติดกันมาเป็นก้อนเดียว (OCR อ่านติด)
            if num_part == "" and cat_part != "":
                # ลองแยกด้วย Regex
                split_match = re.match(r"([0-9]*[ก-ฮ]+[0-9]*)\s*([0-9]+)", cat_part)
                if split_match:
                    cat_part = split_match.group(1)
                    num_part = split_match.group(2)

            # รวม หมวด + เลข
            plate_num = f"{cat_part} {num_part}".strip()
            
            # จัดการจังหวัด
            province = province_part.strip()
            if province == "": province = "ไม่ระบุ"

            # ถ้าอ่านเจอเลขทะเบียน (ต้องมีเลขอย่างน้อย 1 ตัว) ให้ส่งข้อมูล
            if num_part != "":
                print(f"🚗 อ่านป้ายได้: {plate_num} | จังหวัด: {province}")

                # ส่งข้อมูลไป Dashboard
                send_to_dashboard(plate_num, province)

            # วาดกรอบ
            cv2.rectangle(img, (x1, y1), (x2, y2), (0, 255, 0), 2)
            cv2.putText(img, plate_num, (x1, y1-10), cv2.FONT_HERSHEY_SIMPLEX, 0.9, (0, 255, 0), 2)

    # บันทึกรูปผลลัพธ์ (ถ้าต้องการดู)
    # cv2.imwrite(f"output_{filename}", img)