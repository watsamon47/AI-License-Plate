import cv2

# ==========================================
# 🔴 แก้ไขตรงนี้เพื่อลองเปลี่ยนกล้อง
# ==========================================
# ลองเปลี่ยนเลข 0 -> 1 -> 2
# หรือใส่ลิงก์ RTSP เช่น "rtsp://..."
CAMERA_SOURCE = 1 
# ==========================================

print(f"กำลังทดสอบเชื่อมต่อ: {CAMERA_SOURCE}")
cap = cv2.VideoCapture(CAMERA_SOURCE)

if not cap.isOpened():
    print("❌ เชื่อมต่อล้มเหลว! (หาไม่เจอ หรือลิงก์ผิด)")
else:
    print("✅ เชื่อมต่อสำเร็จ! (กำลังอ่านภาพ...)")
    ret, frame = cap.read()
    if ret:
        print("📸 อ่านภาพได้ปกติครับ!")
    else:
        print("⚠️ เชื่อมได้ แต่อ่านภาพไม่ได้ (จอดำ)")

cap.release()